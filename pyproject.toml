[project]
name = "multi-agent-crypto-signals"
version = "0.1.0"
description = "Signal-only multi-agent crypto market research system"
requires-python = ">=3.11"

[project.scripts]
crypto-signals = "crypto_signals.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
